{
  "name": "oryoki",
  "version": "0.0.1",
  "description": "A just-enough web browser.",
  "main": "main.js",
  "scripts": {
    "start": "electron src/js/main/_main.js",
    "build": "electron-packager . Oryoki --out=build --app-version=0.0.1 --platform=darwin --arch=x64 --version=0.36.0 --icon=icon.icns"
  },
  "devDependencies": {
    "electron-localshortcut": "^0.6.0",
    "electron-packager": "^5.2.0",
    "electron-prebuilt": "^0.36.0",
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-exec": "^2.1.2",
    "gulp-livereload": "^3.8.1",
    "gulp-run": "^1.6.12",
    "gulp-sass": "^2.1.1"
  }
}
